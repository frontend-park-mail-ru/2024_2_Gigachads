export function iframe(actionName) {
    // const iframe = document.createElement('iframe');
    // iframe.classList.add('iframe');
    // iframe.src = `/action/${actionName}`;
    // document.body.appendChild(iframe);
}